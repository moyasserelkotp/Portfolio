<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mohamed Yasser - Elite Flutter Developer</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"/>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick-theme.css"/>
    <style>
        /* General Reset & Variables */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif; /* Default body font */
        }

        :root {
            --primary-color: #6A1B9A;
            --primary-light: #8E24AA;
            --primary-dark: #4A148C;
            --secondary-color: #9C27B0;
            --accent-color: #EC4899;

            --text-primary-light: #1a202c;
            --text-secondary-light: #4a5568;
            --text-muted-light: #718096;
            --bg-color-light: #ffffff;
            --bg-secondary-light: #F3E5F5;
            --border-color-light: #e2e8f0;

            /* Dark Theme Variables */
            --text-primary-dark: #f1f5f9; /* Light White */
            --text-secondary-dark: #cbd5e1; /* Off-white */
            --text-muted-dark: #64748b; /* Blue Grey */
            --bg-color-dark: #0f172a; /* Dark Blue */
            --bg-secondary-dark: #1e293b; /* Slightly lighter Dark Blue */
            --border-color-dark: #334155;

            /* Current Theme Variables (dynamic) */
            --bg-color: var(--bg-color-light);
            --bg-secondary: var(--bg-secondary-light);
            --text-primary: var(--text-primary-light);
            --text-secondary: var(--text-secondary-light);
            --text-muted: var(--text-muted-light);
            --border-color: var(--border-color-light);

            /* Shadows */
            --shadow-light: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-medium: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --shadow-heavy: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            --glow-shadow: 0 0 15px rgba(106, 27, 154, 0.4);
        }

        [data-theme="dark"] {
            --bg-color: var(--bg-color-dark);
            --bg-secondary: var(--bg-secondary-dark);
            --text-primary: var(--text-primary-dark);
            --text-secondary: var(--text-secondary-dark);
            --text-muted: var(--text-muted-dark);
            --border-color: var(--border-color-dark);
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-primary);
            line-height: 1.7;
            overflow-x: hidden;
            transition: background-color 0.4s ease, color 0.4s ease;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Poppins', sans-serif; /* Headings font */
            color: var(--text-primary);
            margin-bottom: 1rem;
        }

        /* Preloader */
        #preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: var(--bg-color);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s ease;
        }
        #preloader.hidden {
            opacity: 0;
            pointer-events: none;
        }
        .spinner {
            width: 60px;
            height: 60px;
            border: 6px solid var(--border-color);
            border-top: 6px solid var(--primary-color);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 0.9rem 0;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-light);
        }

        [data-theme="dark"] nav {
            background: rgba(15, 23, 42, 0.9);
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 2.5rem;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            text-decoration: none;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -1px;
        }

        .nav-links {
            display: flex;
            gap: 2.2rem;
            list-style: none;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--text-secondary);
            font-weight: 600;
            transition: color 0.3s ease, transform 0.2s ease;
            position: relative;
            padding: 0.2rem 0;
        }

        .nav-links a:hover {
            color: var(--primary-color);
            transform: translateY(-2px);
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 50%;
            width: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            border-radius: 2px;
            transition: width 0.3s ease, left 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
            left: 0;
        }

        .theme-toggle {
            background: var(--bg-secondary);
            border: 2px solid var(--border-color);
            border-radius: 50%;
            width: 45px;
            height: 45px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            color: var(--text-primary);
        }

        .theme-toggle:hover {
            border-color: var(--primary-color);
            background-color: var(--primary-color);
            color: white;
            transform: scale(1.05) rotate(10deg);
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(145deg, var(--bg-color) 0%, var(--bg-secondary) 100%);
            position: relative;
            overflow: hidden;
            padding-top: 100px;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 70% 30%, rgba(99, 102, 241, 0.08) 0%, transparent 40%),
                        radial-gradient(circle at 20% 80%, rgba(168, 85, 247, 0.08) 0%, transparent 40%);
            animation: background-pan 20s linear infinite alternate;
            z-index: 0;
        }

        @keyframes background-pan {
            0% { background-position: 0% 0%; }
            100% { background-position: 100% 100%; }
        }

        .hero-content {
            text-align: center;
            max-width: 900px;
            padding: 0 2.5rem;
            z-index: 1;
            position: relative;
        }

        .hero-title {
            font-size: 4.5rem;
            font-weight: 800;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1.1;
            animation: scaleIn 1s ease-out;
        }

        .hero-subtitle {
            font-size: 1.8rem;
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
            animation: fadeInUp 1s ease-out 0.2s both;
        }

        .hero-description {
            font-size: 1.15rem;
            color: var(--text-muted);
            margin-bottom: 2.5rem;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
            animation: fadeInUp 1s ease-out 0.4s both;
        }

        .cta-buttons {
            display: flex;
            gap: 1.2rem;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease-out 0.6s both;
        }

        .btn {
            padding: 0.9rem 2.5rem;
            border: none;
            border-radius: 50px;
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.7rem;
            position: relative;
            overflow: hidden;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            box-shadow: var(--shadow-medium);
        }

        .btn-primary:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: var(--shadow-heavy), var(--glow-shadow);
        }

        .btn-secondary {
            background: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
        }

        .btn-secondary:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-4px) scale(1.02);
            box-shadow: var(--shadow-medium);
        }

        /* General Animations */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(40px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes scaleIn {
            from { opacity: 0; transform: scale(0.9); }
            to { opacity: 1; transform: scale(1); }
        }
        @keyframes slideInLeft {
            from { opacity: 0; transform: translateX(-100px); }
            to { opacity: 1; transform: translateX(0); }
        }
        @keyframes slideInRight {
            from { opacity: 0; transform: translateX(100px); }
            to { opacity: 1; transform: translateX(0); }
        }
        @keyframes zoomIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }

        /* Sections */
        .section {
            padding: 6rem 0;
            max-width: 1200px;
            margin: 0 auto;
            padding-left: 2.5rem;
            padding-right: 2.5rem;
            position: relative;
            z-index: 1;
        }

        .section-title {
            text-align: center;
            font-size: 3.2rem;
            font-weight: 700;
            margin-bottom: 4rem;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            padding-bottom: 15px;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 5px;
            background: var(--accent-color);
            border-radius: 5px;
        }

        /* About Section */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 4rem;
            align-items: center;
        }

        .about-image {
            width: 350px;
            height: 350px;
            border-radius: 40px;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            padding: 10px; /* To show the gradient border */
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
            box-shadow: var(--shadow-heavy);
            transform: rotate(-3deg);
            transition: transform 0.4s ease-in-out;
            animation: float 6s ease-in-out infinite;
        }
        .about-image:hover {
            transform: rotate(0deg) scale(1.03);
            animation-play-state: paused;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(-3deg); }
            50% { transform: translateY(-15px) rotate(-3deg); }
        }

        .profile-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 35px;
            display: block;
        }

        .about-text {
            font-size: 1.1rem;
            line-height: 1.8;
            color: var(--text-secondary);
        }
        .about-text p {
            margin-bottom: 1.2rem;
        }

        /* Stats */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 2.5rem;
            margin-top: 3.5rem;
            padding-top: 2.5rem;
            border-top: 1px dashed var(--border-color);
        }

        .stat-card {
            background: var(--bg-secondary);
            padding: 2.2rem;
            border-radius: 20px;
            border: 1px solid var(--border-color);
            text-align: center;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            box-shadow: var(--shadow-light);
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            transform: translateX(-100%);
            transition: transform 0.4s ease-out;
        }
        .stat-card:hover::before {
            transform: translateX(0);
        }

        .stat-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: var(--shadow-medium);
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
            line-height: 1;
        }

        .stat-label {
            color: var(--text-muted);
            font-size: 1rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Skills Section */
        .skills-category {
            margin-bottom: 3rem;
        }

        .skills-category-title {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
            text-align: center;
            margin-bottom: 2rem;
            position: relative;
            padding-bottom: 10px;
        }

        .skills-category-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 3px;
            background: var(--accent-color);
            border-radius: 5px;
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 1.5rem;
            margin-top: 2.5rem;
        }

        .skill-card {
            background: var(--bg-secondary);
            padding: 1.5rem;
            border-radius: 15px;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            text-align: center;
            box-shadow: var(--shadow-light);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .skill-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: var(--shadow-medium);
            background: linear-gradient(145deg, var(--bg-secondary), var(--bg-color));
        }

        .skill-icon {
            font-size: 2.5rem;
            margin-bottom: 0.8rem;
            color: var(--primary-color);
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .skill-name {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--text-primary);
        }

        .about-details {
            margin-top: 2rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            font-size: 1rem;
            color: var(--text-secondary);
        }

        .about-details strong {
            color: var(--primary-color);
            font-weight: 700;
        }

        /* What I'm Learning Section */
        .learning-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2.5rem;
        }

        .learning-card {
            background: var(--bg-secondary);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
            box-shadow: var(--shadow-light);
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .learning-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-medium);
            border-color: var(--primary-light);
        }

        .learning-icon {
            font-size: 2.5rem;
            color: var(--accent-color);
        }

        .learning-info h3 {
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 0.3rem;
        }

        .learning-info p {
            font-size: 0.95rem;
            color: var(--text-muted);
        }

        /* Projects Section */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); /* Adjusted minmax */
            gap: 2.5rem;
            margin-top: 2.5rem;
        }

        /* Animated underline for section title */
        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 5px;
            background: var(--accent-color);
            border-radius: 5px;
            transition: width 0.4s ease-in-out; /* Added transition */
        }
        .section-title:hover::after {
            width: 120px; /* Animate on hover */
        }

        .project-card {
            background: var(--bg-secondary);
            border-radius: 20px; /* As requested */
            border: 1px solid var(--border-color);
            transition: all 0.3s ease-in-out;
            position: relative;
            box-shadow: var(--shadow-medium); /* Softer shadow */
            display: flex;
            flex-direction: column;
            padding: 2rem;
            text-align: center; /* Center align content */
        }

        .project-card:hover {
            transform: scale(1.03); /* Scale up on hover */
            box-shadow: var(--shadow-heavy); /* Lift shadow on hover */
            border-color: var(--primary-light);
        }

        .project-icon {
            font-size: 3rem; /* Tech icon */
            margin-bottom: 1rem;
            color: var(--primary-color);
        }

        .project-content {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .project-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            color: var(--text-primary);
        }

        .project-description {
            color: var(--text-muted);
            margin-bottom: 1.5rem;
            line-height: 1.6;
            font-size: 0.95rem;
            min-height: 57px; /* Approx 3 lines */
            overflow: hidden;
        }

        .project-links {
            margin-top: auto;
            justify-content: center; /* Center the link */
            display: flex;
        }

        .project-link {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1rem;
            padding: 0.5rem 1rem;
            border: 2px solid transparent;
            border-radius: 50px;
        }

        .project-link:hover {
            color: var(--primary-dark);
            background-color: var(--bg-color);
            border-color: var(--primary-dark);
            transform: translateY(-3px);
        }

        /* Testimonials Section */
        .testimonials-container {
            margin-top: 3rem;
            position: relative;
            padding: 0 1rem; /* Adjust for slick carousel default margin */
        }

        .testimonial-card {
            background: var(--bg-secondary);
            padding: 2.5rem;
            border-radius: 20px;
            border: 1px solid var(--border-color);
            margin: 0 1rem; /* Needed for Slick Carousel spacing */
            position: relative;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-light);
        }

        .testimonial-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-medium);
        }

        .testimonial-content {
            font-style: italic;
            color: var(--text-secondary);
            margin-bottom: 2rem;
            position: relative;
            line-height: 1.8;
            font-size: 1.05rem;
        }

        .testimonial-content::before,
        .testimonial-content::after {
            content: '"';
            font-size: 4rem;
            color: var(--primary-color);
            opacity: 0.2;
            position: absolute;
            font-family: serif;
        }

        .testimonial-content::before {
            top: -25px;
            left: -15px;
        }

        .testimonial-content::after {
            bottom: -35px;
            right: -15px;
        }

        .testimonial-author {
            display: flex;
            align-items: center;
            gap: 1.2rem;
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 1px dotted var(--border-color);
        }

        .author-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--primary-light);
            box-shadow: var(--shadow-light);
        }

        .author-info {
            display: flex;
            flex-direction: column;
        }

        .author-name {
            font-weight: 700;
            font-size: 1.1rem;
        }

        .author-role {
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        /* Slick Carousel Custom Dots */
        .slick-dots {
            bottom: -60px !important;
        }
        .slick-dots li button:before {
            color: var(--primary-color) !important;
            font-size: 10px !important;
            opacity: 0.5 !important;
        }
        .slick-dots li.slick-active button:before {
            color: var(--primary-dark) !important;
            opacity: 1 !important;
        }

        /* Timeline Section */
        .timeline {
            position: relative;
            max-width: 900px;
            margin: 0 auto;
            padding: 2rem 0;
        }

        .timeline::before {
            content: '';
            position: absolute;
            left: 50%;
            top: 0;
            width: 4px;
            height: 100%;
            background: var(--border-color);
            transform: translateX(-50%);
            z-index: 0;
        }

        .timeline-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 3rem;
            position: relative;
        }

        .timeline-item:nth-child(even) {
            flex-direction: row-reverse;
        }

        .timeline-item-content {
            width: calc(50% - 30px);
            background: var(--bg-secondary);
            padding: 2rem;
            border-radius: 15px;
            border: 1px solid var(--border-color);
            box-shadow: var(--shadow-light);
            transition: all 0.3s ease;
        }

        .timeline-item-content:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-medium);
            border-color: var(--primary-light);
        }

        .timeline-item-content h3 {
            font-size: 1.4rem;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .timeline-item-content p {
            font-size: 0.95rem;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
        }

        .timeline-item-content span {
            font-size: 0.85rem;
            color: var(--text-muted);
            display: block;
            margin-top: 0.5rem;
        }

        .timeline-icon {
            position: absolute;
            left: 50%;
            top: 0;
            transform: translateX(-50%);
            width: 50px;
            height: 50px;
            background: var(--primary-color);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: var(--shadow-medium);
            z-index: 1;
        }

        .resume-download-btn {
            text-align: center;
            margin-top: 3rem;
        }

        /* Contact Section */
        .contact-content {
            display: grid;
            grid-template-columns: 1.2fr 1.8fr;
            gap: 4rem;
            align-items: start;
        }

        .contact-info, .contact-form {
            background: var(--bg-secondary);
            padding: 3rem;
            border-radius: 20px;
            border: 1px solid var(--border-color);
            box-shadow: var(--shadow-medium);
        }

        .contact-info h3, .contact-form h3 {
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            color: var(--primary-color);
        }

        .contact-info p {
            margin-bottom: 1rem;
            color: var(--text-secondary);
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }
        .contact-info p i {
            color: var(--primary-light);
            font-size: 1.2rem;
        }

        .form-group {
            margin-bottom: 1.8rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.8rem;
            font-weight: 600;
            color: var(--text-secondary);
            font-size: 1rem;
        }

        .form-input,
        .form-textarea {
            width: 100%;
            padding: 1rem;
            border: 2px solid var(--border-color);
            border-radius: 12px;
            background: var(--bg-color);
            color: var(--text-primary);
            font-size: 1rem;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-input:focus,
        .form-textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
        }

        .form-error {
            color: var(--accent-color);
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }

        .form-input.is-invalid,
        .form-textarea.is-invalid {
            border-color: var(--accent-color);
        }

        .form-textarea {
            resize: vertical;
            min-height: 150px;
        }

        .social-links {
            display: flex;
            gap: 1.2rem;
            margin-top: 2.5rem;
        }

        .social-link {
            width: 55px;
            height: 55px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-decoration: none;
            font-size: 1.8rem;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-light);
        }

        .social-link:hover {
            transform: translateY(-5px) rotate(5deg) scale(1.05);
            box-shadow: var(--shadow-medium);
            background: linear-gradient(135deg, var(--secondary-color), var(--accent-color));
        }

        /* Footer */
        footer {
            background: var(--bg-secondary);
            padding: 2.5rem 0;
            text-align: center;
            color: var(--text-muted);
            border-top: 1px solid var(--border-color);
            font-size: 0.9rem;
        }

        .hamburger-menu {
            display: none;
            font-size: 1.8rem;
            cursor: pointer;
            color: var(--primary-color);
            background: transparent;
            border: none;
            padding: 0.5rem;
            border-radius: 8px;
            transition: transform 0.3s ease;
        }

        .hamburger-menu:hover {
            transform: scale(1.1);
        }

        .mobile-nav-links {
            display: none;
            position: fixed;
            top: 70px;
            left: 0;
            width: 100%;
            background: var(--bg-secondary);
            border-top: 1px solid var(--border-color);
            padding: 1rem 0;
            flex-direction: column;
            text-align: center;
            z-index: 999;
        }

        .mobile-nav-links.active {
            display: flex;
        }

        .mobile-nav-links li {
            list-style: none;
            padding: 0.8rem 0;
        }

        .mobile-nav-links a {
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 500;
            font-size: 1.1rem;
            transition: color 0.3s ease;
        }

        .mobile-nav-links a:hover {
            color: var(--primary-color);
        }

        /* Mobile Responsive */
        @media (max-width: 992px) {
            .nav-links {
                display: none;
            }
            .hamburger-menu {
                display: block;
            }
            .hero-title {
                font-size: 3.5rem;
            }
            .hero-subtitle {
                font-size: 1.5rem;
            }
            .about-content {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 2.5rem;
            }
            .about-image {
                transform: rotate(0deg); /* Reset rotation on mobile */
            }
            .about-image:hover {
                transform: scale(1.03); /* Keep hover effect simpler */
            }
            @keyframes float {
                0%, 100% { transform: translateY(0); }
                50% { transform: translateY(-15px); }
            }

            .contact-content {
                grid-template-columns: 1fr;
            }
            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
            .section {
                padding: 4rem 1.5rem;
            }
            .section-title {
                font-size: 2.5rem;
            }
            .stats-container, .skills-grid, .projects-grid, .learning-grid {
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            }
            .stat-card, .skill-card, .project-card, .learning-card {
                padding: 1.8rem;
            }
            .testimonial-card {
                margin: 0 0.5rem;
            }
        }

        @media (max-width: 576px) {
            .hero-title {
                font-size: 2.8rem;
            }
            .hero-subtitle {
                font-size: 1.3rem;
            }
            .cta-buttons .btn {
                width: 90%;
            }
            .section-title {
                font-size: 2rem;
                margin-bottom: 3rem;
            }
            .about-image {
                width: 280px;
                height: 280px;
            }
            .stats-container {
                grid-template-columns: 1fr;
            }
            .learning-card {
                flex-direction: column;
                text-align: center;
                gap: 1rem;
            }
            .learning-icon {
                margin-bottom: 0.5rem;
            }
            .project-card {
                min-width: unset;
                width: 100%;
            }
            .testimonial-content {
                font-size: 1rem;
            }
            .contact-info, .contact-form {
                padding: 2rem;
            }
        }


        /* Animate on scroll classes */
        .animate-on-scroll {
            opacity: 0;
            transition: all 0.9s cubic-bezier(0.25, 0.46, 0.45, 0.94); /* Smoother transition */
        }
        .animate-on-scroll.animate-fade-in {
            opacity: 1;
        }
        .animate-on-scroll.animate-slide-in-left {
            opacity: 1;
            transform: translateX(0);
        }
        .animate-on-scroll.animate-slide-in-right {
            opacity: 1;
            transform: translateX(0);
        }
        .animate-on-scroll.animate-zoom-in {
            opacity: 1;
            transform: scale(1);
        }
    </style>
</head>
<body>
    <div id="preloader">
        <div class="spinner"></div>
    </div>

    <nav>
        <div class="nav-container">
            <a href="#" class="logo">Mohamed Yasser</a>
            <ul class="nav-links">
                <li><a href="#hero">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#skills">Skills</a></li>
                <li><a href="#projects">Projects</a></li>
                <li><a href="#experience">Experience</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            <div class="nav-controls">
                <button id="theme-toggle" class="theme-toggle" aria-label="Toggle theme">
                    <i class="fas fa-moon" id="theme-icon"></i>
                </button>
                <button class="hamburger-menu" aria-label="Toggle navigation menu">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>
        <ul class="mobile-nav-links">
            <li><a href="#hero">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#skills">Skills</a></li>
            <li><a href="#projects">Projects</a></li>
            <li><a href="#experience">Experience</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <main>
        <section id="hero" class="hero">
            <div class="hero-content">
                <h1 class="hero-title">Hi, I'm Mohamed Yasser.</h1>
                <p class="hero-subtitle">An Elite Flutter Developer</p>
                <p class="hero-description">
                    Passionate about crafting beautiful and highly functional mobile applications.
                    I transform ideas into seamless and engaging user experiences.
                </p>
                <div class="cta-buttons">
                    <a href="#projects" class="btn btn-primary">
                        <i class="fas fa-eye"></i> View My Work
                    </a>
                    <a href="https://docs.google.com/document/d/1Lv7c0Kl9BH7uFZlmPtwdWA0OlsfIRI3j/edit?usp=drive_link&ouid=106633612953213316002&rtpof=true&sd=true" download class="btn btn-secondary">
                        <i class="fas fa-envelope"></i> Get In Touch
                    </a>
                </div>
            </div>
        </section>

    <section id="about" class="section">
        <h2 class="section-title">About Me</h2>
        <div class="about-content">
            <div class="about-image">
                <img src="https://avatars.githubusercontent.com/u/42767131?v=4" alt="Mohamed Yasser" class="profile-image">
            </div>
            <div class="about-text">
                <p>
                    Hello! I'm Mohamed Yasser, a dedicated and innovative Flutter developer with a strong passion for building
                    cross-platform mobile applications. My journey in development began with a fascination for
                    creating intuitive user interfaces and robust backend systems.
                </p>
                <p>
                    With a solid foundation in Flutter, Dart, and related technologies, I specialize in
                    turning complex ideas into elegant and efficient mobile solutions. I thrive on solving challenges
                    and continuously learning new technologies to deliver high-quality products.
                </p>
                <p>
                    Beyond coding, I'm a firm believer in clean architecture, test-driven development, and collaborative environments.
                    I'm always looking for opportunities to contribute to impactful projects and grow as a developer.
                </p>
                <div class="about-details">
                </div>
            </div>
        </div>
        <div class="stats-container">
            <div class="stat-card">
                <div class="stat-number">5+</div>
                <div class="stat-label">Years Experience</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">20+</div>
                <div class="stat-label">Projects Completed</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">10+</div>
                <div class="stat-label">Happy Clients</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">1k+</div>
                <div class="stat-label">Cups of Coffee</div>
            </div>
        </div>
    </section>

    <section id="skills" class="section">
        <h2 class="section-title">My Skills</h2>
        <div class="skills-category">
            <h3 class="skills-category-title">Frontend Development (Flutter)</h3>
            <div class="skills-grid">
                <div class="skill-card">
                    <i class="fab fa-dev skill-icon"></i>
                    <span class="skill-name">Flutter</span>
                </div>
                <div class="skill-card">
                        <i class="fas fa-code skill-icon"></i>
                        <span class="skill-name">Dart</span>
                    </div>
                    <div class="skill-card">
                        <i class="fas fa-mobile-alt skill-icon"></i>
                        <span class="skill-name">Responsive Design</span>
                    </div>
                <div class="skill-card">
                    <i class="fas fa-cubes skill-icon"></i>
                    <span class="skill-name">State Management (Provider, BLoC)</span>
                </div>
                <div class="skill-card">
                    <i class="fas fa-network-wired skill-icon"></i>
                    <span class="skill-name">API Integration</span>
                </div>
            </div>
        </div>

        <div class="skills-category">
            <h3 class="skills-category-title">Backend & Database</h3>
            <div class="skills-grid">
                <div class="skill-card">
                    <i class="fas fa-fire skill-icon"></i>
                    <span class="skill-name">Firebase</span>
                </div>
                <div class="skill-card">
                    <i class="fas fa-database skill-icon"></i>
                    <span class="skill-name">SQL/NoSQL</span>
                </div>
                <div class="skill-card">
                    <i class="fas fa-server skill-icon"></i>
                    <span class="skill-name">RESTful APIs</span>
                </div>
            </div>
        </div>

        <div class="skills-category">
            <h3 class="skills-category-title">Tools & Others</h3>
            <div class="skills-grid">
                <div class="skill-card">
                    <i class="fab fa-git-alt skill-icon"></i>
                    <span class="skill-name">Git</span>
                </div>
                <div class="skill-card">
                    <i class="fab fa-github skill-icon"></i>
                    <span class="skill-name">GitHub</span>
                </div>
                <div class="skill-card">
                    <i class="fas fa-code-branch skill-icon"></i>
                    <span class="skill-name">Version Control</span>
                </div>
                <div class="skill-card">
                    <i class="fas fa-bug skill-icon"></i>
                    <span class="skill-name">Debugging</span>
                </div>
                <div class="skill-card">
                    <i class="fas fa-project-diagram skill-icon"></i>
                    <span class="skill-name">Agile Methodologies</span>
                </div>
            </div>
        </div>
    </section>

    <section id="learning" class="section">
        <h2 class="section-title">What I'm Learning</h2>
        <div class="learning-grid">
            <div class="learning-card">
                <div class="learning-icon"><i class="fas fa-rocket"></i></div>
                <div class="learning-info">
                    <h3>Advanced Flutter Animations</h3>
                    <p>Exploring complex UI animations and transitions to create more dynamic and engaging user experiences.</p>
                </div>
            </div>
            <div class="learning-card">
                <div class="learning-icon"><i class="fas fa-server"></i></div>
                <div class="learning-info">
                    <h3>Server-Side Dart</h3>
                    <p>Learning to build scalable and efficient backends with Dart, enabling full-stack development with a single language.</p>
                </div>
            </div>
            <div class="learning-card">
                <div class="learning-icon"><i class="fas fa-cogs"></i></div>
                <div class="learning-info">
                    <h3>WebAssembly with Dart</h3>
                    <p>Investigating how to compile Dart to WebAssembly for high-performance web applications.</p>
                </div>
            </div>
        </div>
    </section>

        <section id="experience" class="section">
            <h2 class="section-title">Experience & Education</h2>
            <div class="timeline">
                <div class="timeline-item">
                    <div class="timeline-item-content">
                        <h3>Senior Flutter Developer</h3>
                        <p>Tech Solutions Inc.</p>
                        <span>Jan 2022 - Present</span>
                        <p>Led development of high-performance mobile apps, mentored junior developers, and implemented new features.</p>
                    </div>
                    <div class="timeline-icon"><i class="fas fa-briefcase"></i></div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-item-content">
                        <h3>Flutter Developer</h3>
                        <p>Innovate Mobile</p>
                        <span>Mar 2019 - Dec 2021</span>
                        <p>Developed and maintained various Flutter applications, collaborated with cross-functional teams.</p>
                    </div>
                    <div class="timeline-icon"><i class="fas fa-laptop-code"></i></div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-item-content">
                        <h3>B.Sc. in Computer Science</h3>
                        <p>University of Alexandria</p>
                        <span>Sep 2015 - Jun 2019</span>
                        <p>Graduated with honors, specialized in software engineering and mobile development.</p>
                    </div>
                    <div class="timeline-icon"><i class="fas fa-graduation-cap"></i></div>
                </div>
            </div>
            <div class="resume-download-btn">
                <a href="https://docs.google.com/document/d/1Lv7c0Kl9BH7uFZlmPtwdWA0OlsfIRI3j/edit?usp=drive_link&ouid=106633612953213316002&rtpof=true&sd=true" class="btn btn-primary" target="_blank">
                    <i class="fas fa-download"></i> Download Resume
                </a>
            </div>
        </section>

    <section id="projects" class="section animate-on-scroll animate-slide-in-left">
            <h2 class="section-title">Featured Projects</h2>
            <div class="projects-grid" id="github-projects-grid">
                <!-- Projects will be loaded here by JavaScript -->
            </div>
        </section>

    <section id="testimonials" class="section">
        <h2 class="section-title">Testimonials</h2>
        <div class="testimonials-container">
            <div class="testimonial-card">
                <div class="testimonial-content">
                    <p>"Mohamed is a highly skilled developer who consistently delivers exceptional results. His attention to detail and commitment to quality are truly impressive. I would highly recommend him for any Flutter project."</p>
                </div>
                <div class="testimonial-author">
                    <img src="https://randomuser.me/api/portraits/men/32.jpg" alt="Author" class="author-avatar">
                    <div class="author-info">
                        <span class="author-name">John Doe</span>
                        <span class="author-role">CEO, Tech Solutions Inc.</span>
                    </div>
                </div>
            </div>
            <div class="testimonial-card">
                <div class="testimonial-content">
                    <p>"Working with Mohamed was a pleasure. He is a clear communicator and a talented developer who is passionate about his work. I look forward to collaborating with him on future projects."</p>
                </div>
                <div class="testimonial-author">
                    <img src="https://randomuser.me/api/portraits/women/44.jpg" alt="Author" class="author-avatar">
                    <div class="author-info">
                        <span class="author-name">Jane Smith</span>
                        <span class="author-role">Project Manager, Innovate Mobile</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    

    <section id="contact" class="section animate-on-scroll animate-slide-in-right">
        <h2 class="section-title">Get in Touch</h2>
        <div class="contact-content">
            <div class="contact-info">
                <h3>Contact Information</h3>
                <p>Feel free to reach out to discuss your project, collaboration opportunities, or just to say hello!</p>
                <p><i class="fas fa-envelope"></i> <strong>Email:</strong> <a href="mailto:mohamedyasser.alkotp@gmail.com" style="color:var(--text-secondary); text-decoration:none;">mohamedyasser.alkotp@gmail.com</a></p>
                <p><i class="fas fa-phone"></i> <strong>Phone:</strong> +20 0127302282</p>
                <p><i class="fas fa-map-marker-alt"></i> <strong>Location:</strong> New Borg El Arab, Alexandria Governorate, Egypt</p>
                <div class="social-links">
                    <a href="https://github.com/moyasserelkotp" target="_blank" class="social-link"><i class="fab fa-github"></i></a>
                    <a href="https://www.linkedin.com/in/mohamed-elkotp-bb9b3b272" target="_blank" class="social-link"><i class="fab fa-linkedin-in"></i></a>
                    <a href="https://twitter.com/M0xYasser" target="_blank" class="social-link"><i class="fab fa-twitter"></i></a>
                    </div>
            </div>
            <div class="contact-form">
                <h3>Send Me a Message</h3>
                <form action="#" method="POST" id="contactForm">
                    <div class="form-group">
                        <label for="name" class="form-label">Name</label>
                        <input type="text" id="name" name="name" class="form-input" required>
                        <div class="form-error"></div>
                    </div>
                    <div class="form-group">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" id="email" name="email" class="form-input" required>
                        <div class="form-error"></div>
                    </div>
                    <div class="form-group">
                        <label for="subject" class="form-label">Subject</label>
                        <input type="text" id="subject" name="subject" class="form-input">
                    </div>
                    <div class="form-group">
                        <label for="message" class="form-label">Message</label>
                        <textarea id="message" name="message" class="form-textarea" required></textarea>
                        <div class="form-error"></div>
                    </div>
                    <button type="submit" class="btn btn-primary">Send Message <i class="fas fa-paper-plane"></i></button>
                    <div id="form-thank-you" style="display:none; margin-top: 1rem; color: var(--primary-color);">Thank you for your message!</div>
                </form>
            </div>
        </div>
    </section>

    <footer>
        <p>&copy; 2024 Mohamed Yasser. All rights reserved.</p>
    </footer>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Preloader
            const preloader = document.getElementById('preloader');
            if (preloader) {
                window.addEventListener('load', () => {
                    preloader.classList.add('hidden');
                });
            }

            // Theme Toggle
            const themeToggle = document.getElementById('theme-toggle');
            const themeIcon = document.getElementById('theme-icon');
            const currentTheme = localStorage.getItem('theme');

            if (currentTheme) {
                document.documentElement.setAttribute('data-theme', currentTheme);
                if (currentTheme === 'dark') {
                    themeIcon.classList.replace('fa-moon', 'fa-sun');
                }
            }
            themeToggle.addEventListener('click', () => {
                let theme = document.documentElement.getAttribute('data-theme');
                if (theme === 'dark') {
                    document.documentElement.setAttribute('data-theme', 'light');
                    localStorage.setItem('theme', 'light');
                    themeIcon.classList.replace('fa-sun', 'fa-moon');
                } else {
                    document.documentElement.setAttribute('data-theme', 'dark');
                    localStorage.setItem('theme', 'dark');
                    themeIcon.classList.replace('fa-moon', 'fa-sun');
                }
            });

            // Hamburger Menu for Mobile
            const hamburgerMenu = document.querySelector('.hamburger-menu');
            const mobileNavLinks = document.querySelector('.mobile-nav-links');

            hamburgerMenu.addEventListener('click', () => {
                mobileNavLinks.classList.toggle('active');
            });

            // Close mobile nav when a link is clicked
            mobileNavLinks.querySelectorAll('a').forEach(link => {
                link.addEventListener('click', () => {
                    mobileNavLinks.classList.remove('active');
                });
            });

            // Smooth Scrolling
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    document.querySelector(this.getAttribute('href')).scrollIntoView({
                        behavior: 'smooth'
                    });
                });
            });

            // Fetch GitHub Projects
            async function fetchGithubProjects() {
                const projectsGrid = document.getElementById('github-projects-grid');
                const projectRepos = [
                    'https://api.github.com/repos/moyasserelkotp/Clean_Architecture-Movies-App-',
                    'https://api.github.com/repos/moyasserelkotp/Bea_shop',
                    'https://api.github.com/repos/moyasserelkotp/MyNote',
                    'https://api.github.com/repos/moyasserelkotp/My_Money',
                    'https://api.github.com/repos/moyasserelkotp/Smart_classs_room',
                    'https://api.github.com/repos/moyasserelkotp/Responsive_Dashboard',
                    'https://api.github.com/repos/moyasserelkotp/trend_block_version_one',
                    'https://api.github.com/repos/moyasserelkotp/BMI_Calculater_update'
                ];

                projectsGrid.innerHTML = '<div class="spinner"></div>'; // Show spinner

                try {
                    const fetchPromises = projectRepos.map(url =>
                        fetch(url).then(response => {
                            if (!response.ok) {
                                console.error(`Error fetching ${url}: ${response.status} ${response.statusText}`);
                                return null;
                            }
                            return response.json();
                        }).catch(error => {
                            console.error(`Network error fetching ${url}:`, error);
                            return null;
                        })
                    );

                    const allReposData = await Promise.all(fetchPromises);
                    const validRepos = allReposData.filter(repo => repo !== null);

                    if (validRepos.length === 0) {
                        projectsGrid.innerHTML = '<p style="text-align: center; color: var(--text-muted);">No projects found.</p>';
                        return;
                    }

                    validRepos.sort((a, b) => new Date(b.updated_at) - new Date(a.updated_at));

                    projectsGrid.innerHTML = ''; // Clear spinner

                    validRepos.forEach(repo => {
                        const projectCard = document.createElement('div');
                        projectCard.classList.add('project-card');

                        const projectName = repo.name.replace(/[-_]/g, ' ');
                        const projectDescription = repo.description ? (repo.description.length > 100 ? repo.description.substring(0, 100) + '...' : repo.description) : 'A brief description of the project.';

                        projectCard.innerHTML = `
                            <div class="project-icon"><i class="fas fa-code"></i></div>
                            <div class="project-content">
                                <h3 class="project-title">${projectName}</h3>
                                <p class="project-description">${projectDescription}</p>
                                <div class="project-links">
                                    <a href="${repo.html_url}" class="project-link" target="_blank">
                                        <i class="fab fa-github"></i> GitHub Repo
                                    </a>
                                </div>
                            </div>
                        `;
                        projectsGrid.appendChild(projectCard);
                    });
                } catch (error) {
                    console.error('Error fetching GitHub projects:', error);
                    projectsGrid.innerHTML = '<p style="text-align: center; color: var(--text-muted);">Failed to load projects.</p>';
                } finally {
                    // Optional: remove spinner if it wasn't replaced
                }
            }

            fetchGithubProjects();

            // Contact Form Validation
            const contactForm = document.getElementById('contactForm');
            contactForm.addEventListener('submit', function(e) {
                e.preventDefault();
                let isValid = true;

                const name = document.getElementById('name');
                const email = document.getElementById('email');
                const message = document.getElementById('message');

                if (name.value.trim() === '') {
                    isValid = false;
                    showError(name, 'Name is required.');
                } else {
                    hideError(name);
                }

                if (email.value.trim() === '') {
                    isValid = false;
                    showError(email, 'Email is required.');
                } else if (!/^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/.test(email.value)) {
                    isValid = false;
                    showError(email, 'Please enter a valid email address.');
                } else {
                    hideError(email);
                }

                if (message.value.trim() === '') {
                    isValid = false;
                    showError(message, 'Message is required.');
                } else {
                    hideError(message);
                }

                if (isValid) {
                    // Replace with actual form submission logic (e.g., AJAX call)
                    console.log('Form submitted successfully!');
                    document.getElementById('form-thank-you').style.display = 'block';
                    contactForm.reset();
                }
            });

            function showError(input, message) {
                const formGroup = input.parentElement;
                const error = formGroup.querySelector('.form-error');
                error.textContent = message;
                input.classList.add('is-invalid');
            }

            function hideError(input) {
                const formGroup = input.parentElement;
                const error = formGroup.querySelector('.form-error');
                error.textContent = '';
                input.classList.remove('is-invalid');
            }
        });
    </script>
</body>
</html>